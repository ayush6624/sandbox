# config.env holds project ID and the Tailscale auth key — never commit it.
# Commit config.env.example (the sanitized template) instead.
config.env
# fleet-deploy.sh writes the generated gateway/host tokens here.
fleet-secrets.env
